class Token{
    var tokenCounter: Int

    init(){
        //empty for now
    }

    func freeRefill() {
        //empty for now
    }

    func payedRefill() {
        //empty for now
    }

    func useToken() {
        //empty for now
    }

    func rewardToken() {
        //empty for now
    }
}
